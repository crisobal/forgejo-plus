= Forgejo + asciidoc + pandoc

Forgejo + is a docker image of forgejo based on the official forgejo docker image. Additionally asciidoctor and pandoc 
packages are installed as external renderer. Additionally the app.ini config template features the config for the external
renderers.
